[package]
name = "qemu-boot"
version = "0.1.0"
edition = "2021"

[dependencies]
object = { version = "0.40.0", default-features = false, features = ["read_core", "macho"] }

[profile.release]
opt-level = "s"
lto = "thin"
panic = "abort"
codegen-units = 1
