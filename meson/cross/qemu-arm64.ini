[constants]
target_triple = 'arm64-apple-macos26.0.0'

[binaries]
c = ['clang', '-target', target_triple, '-fuse-ld=lld']
cpp = ['clang++', '-target', target_triple, '-fuse-ld=lld']
ar = 'llvm-ar'
strip = 'llvm-strip'

[built-in options]
c_args = ['-mcpu=cortex-a53']
cpp_args = ['-mcpu=cortex-a53']

[properties]
target_triple = target_triple

[host_machine]
system = 'darwin'
cpu_family = 'aarch64'
cpu = 'cortex-a53'
endian = 'little'
