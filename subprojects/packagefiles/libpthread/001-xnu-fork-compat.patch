diff --git a/kern/kern_internal.h b/kern/kern_internal.h
index 156ad74..ebe4fe6 100644
--- a/kern/kern_internal.h
+++ b/kern/kern_internal.h
@@ -138,6 +138,8 @@ struct _pthread_registration_data {
 
 extern pthread_callbacks_t pthread_kern;
 
+#ifndef _KSYN_WAITQ_ELEMENT_DEFINED
+#define _KSYN_WAITQ_ELEMENT_DEFINED
 struct ksyn_waitq_element {
 	TAILQ_ENTRY(ksyn_waitq_element) kwe_list;	/* link to other list members */
 	void *          kwe_kwqqueue;			/* queue blocked on */
@@ -149,14 +151,12 @@ struct ksyn_waitq_element {
 	uint32_t	kwe_psynchretval;		/* thread retval */
 	void		*kwe_uth;			/* uthread */
 };
+#endif
 typedef struct ksyn_waitq_element * ksyn_waitq_element_t;
 
 #define PTH_DEFAULT_STACKSIZE 512*1024
 #define MAX_PTHREAD_SIZE 64*1024
 
-/* exported from the kernel but not present in any headers. */
-extern thread_t port_name_to_thread(mach_port_name_t port_name);
-
 /* function declarations for pthread_kext.c */
 void pthread_init(void);
 void _pth_proc_hashinit(proc_t p);
diff --git a/kern/kern_support.c b/kern/kern_support.c
index bdb04ab..81bdcdd 100644
--- a/kern/kern_support.c
+++ b/kern/kern_support.c
@@ -98,6 +98,7 @@ extern void panic(const char *string, ...) __printflike(1,2) __dead2;
 //#include <vm/vm_protos.h>
 #include <vm/vm_fault.h>
 #include <vm/vm_map.h>
+#include <mach/vm_statistics.h>
 #include <mach/thread_act.h> /* for thread_resume */
 #include <machine/machine_routines.h>
 #include <mach/shared_region.h>
@@ -816,7 +817,7 @@ workq_set_register_state(proc_t p, thread_t th,
 
 		int error = pthread_kern->thread_set_wq_state32(th, (thread_state_t)&state);
 		if (error != KERN_SUCCESS) {
-			panic(__func__ ": thread_set_wq_state failed: %d", error);
+			panic("thread_set_wq_state failed: %d", error);
 		}
 	} else {
 		x86_thread_state64_t state64 = {
@@ -834,7 +835,7 @@ workq_set_register_state(proc_t p, thread_t th,
 
 		int error = pthread_kern->thread_set_wq_state64(th, (thread_state_t)&state64);
 		if (error != KERN_SUCCESS) {
-			panic(__func__ ": thread_set_wq_state failed: %d", error);
+			panic("thread_set_wq_state failed: %d", error);
 		}
 	}
 #elif defined(__arm__) || defined(__arm64__)
@@ -854,7 +855,7 @@ workq_set_register_state(proc_t p, thread_t th,
 
 		int error = pthread_kern->thread_set_wq_state32(th, (thread_state_t)&state);
 		if (error != KERN_SUCCESS) {
-			panic(__func__ ": thread_set_wq_state failed: %d", error);
+			panic("thread_set_wq_state failed: %d", error);
 		}
 	} else {
 #if defined(__arm64__)
@@ -872,7 +873,7 @@ workq_set_register_state(proc_t p, thread_t th,
 
 		int error = pthread_kern->thread_set_wq_state64(th, (thread_state_t)&state);
 		if (error != KERN_SUCCESS) {
-			panic(__func__ ": thread_set_wq_state failed: %d", error);
+			panic("thread_set_wq_state failed: %d", error);
 		}
 #else /* defined(__arm64__) */
 		panic("Shouldn't have a 64-bit thread on a 32-bit kernel...");
@@ -967,9 +968,9 @@ workq_setup_thread(proc_t p, thread_t th, vm_map_t map, user_addr_t stackaddr,
 		vm_map_offset_t th_page = vm_map_trunc_page_mask(th_addrs.self, mask);
 		vm_map_offset_t stk_page = vm_map_trunc_page_mask(th_addrs.stack_top - 1, mask);
 		if (th_page != stk_page) {
-			vm_fault(map, stk_page, VM_PROT_READ | VM_PROT_WRITE, FALSE, THREAD_UNINT, NULL, 0);
+			vm_fault(map, stk_page, VM_PROT_READ | VM_PROT_WRITE, FALSE, VM_KERN_MEMORY_NONE, THREAD_UNINT, NULL, 0);
 		}
-		vm_fault(map, th_page, VM_PROT_READ | VM_PROT_WRITE, FALSE, THREAD_UNINT, NULL, 0);
+		vm_fault(map, th_page, VM_PROT_READ | VM_PROT_WRITE, FALSE, VM_KERN_MEMORY_NONE, THREAD_UNINT, NULL, 0);
 	}
 
 	if (setup_flags & WQ_SETUP_EXIT_THREAD) {
@@ -1049,5 +1050,6 @@ _pthread_init(void)
 		pthread_mutex_default_policy = policy_bootarg;
 	}
 
-	sysctl_register_oid(&sysctl__kern_pthread_mutex_default_policy);
+	// sysctl__kern_pthread_mutex_default_policy is auto-registered via SYSCTL_INT -> STARTUP(SYSCTL)
+	// sysctl_register_oid(&sysctl__kern_pthread_mutex_default_policy);
 }
diff --git a/kern/kern_synch.c b/kern/kern_synch.c
index 2eb0489..f856461 100644
--- a/kern/kern_synch.c
+++ b/kern/kern_synch.c
@@ -1069,7 +1069,7 @@ __psynch_cvsignal(user_addr_t cv, uint32_t cgen, uint32_t cugen,
 	}
 	
 	if (threadport != 0) {
-		th = port_name_to_thread((mach_port_name_t)threadport);
+		th = port_name_to_thread((mach_port_name_t)threadport, PORT_INTRANS_THREAD_IN_CURRENT_TASK);
 		if (th == THREAD_NULL) {
 			return ESRCH;
 		}
